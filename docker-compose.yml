# SubLearn: два модуля
#   ollama — модели (CPU в Docker VM; на Mac Metal см. ниже)
#   web    — API, статика, HLS-прокси
#
# Две модели: лёгкая (кнопки «ИИ») + тяжёлая (эскалация). В RAM одна за раз, ~3 ГБ.
#
# Запуск:  ./start.sh   или   docker compose up -d --build
# Стоп:    docker compose down
# Логи:    docker compose logs -f web ollama
# Чистка моделей: docker compose down -v
#
# Опционально быстрее на Mac (Metal): поставьте Ollama.app, pull qwen3:1.7b и qwen3:4b,
# затем поднимите только web:
#   docker compose up -d --build web
#   # и в environment web:
#   # SUBLEARN_OLLAMA_URL: http://host.docker.internal:11434
#   # (сервис ollama можно остановить: docker compose stop ollama)

services:
  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama_data:/root/.ollama
    environment:
      OLLAMA_HOST: 0.0.0.0:11434
      # 3m = тёплый idle: после AI/prefetch модель в RAM ~3 мин, потом сама выгружается.
      OLLAMA_KEEP_ALIVE: "3m"
      OLLAMA_MAX_LOADED_MODELS: "1"
      OLLAMA_NUM_PARALLEL: "1"
    expose:
      - "11434"
    cpus: "2.0"
    mem_limit: 3g
    healthcheck:
      test: ["CMD-SHELL", "ollama list >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 10s
      retries: 36
      start_period: 10s
    restart: unless-stopped

  web:
    build: .
    ports:
      - "8765:8765"
    dns:
      - 1.1.1.1
      - 8.8.8.8
    volumes:
      - vocab_data:/app/data
      - ./server.py:/app/server.py
      - ./backend:/app/backend
      - ./app.js:/app/app.js
      - ./frontend:/app/frontend
      - ./index.html:/app/index.html
      - ./styles.css:/app/styles.css
      - ./ad-skip.js:/app/ad-skip.js
      - ./entrypoint.sh:/entrypoint.sh
    environment:
      SUBLEARN_OLLAMA_URL: http://ollama:11434
      SUBLEARN_OLLAMA_MODEL: qwen3:4b
      SUBLEARN_OLLAMA_MODEL_LIGHT: qwen3:1.7b
      SUBLEARN_OLLAMA_NUM_THREAD: "2"
      SUBLEARN_HOST: 0.0.0.0
      SUBLEARN_PORT: "8765"
      SUBLEARN_DATA_DIR: /app/data
      SUBLEARN_NEWDEAF_DNS: cloudflare
      OLLAMA_KEEP_ALIVE: "3m"
    depends_on:
      ollama:
        condition: service_healthy
    cpus: "1.0"
    mem_limit: 768m
    restart: unless-stopped

volumes:
  ollama_data:
  vocab_data:
